<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ElectionMobileApp.Views.SurveyHomePage"
             Title="Survey">
    <ContentPage.Content>
        <StackLayout>
            <ListView x:Name="lsSurvey"
                VerticalOptions="FillAndExpand"
                HasUnevenRows="true"
                  ItemTapped="LsSurvey_ItemTappedAsync"
                IsPullToRefreshEnabled="true"
                IsRefreshing="{Binding IsBusy, Mode=OneWay}"
                CachingStrategy="RecycleElement">

                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell>
                            <StackLayout Padding="10">
                                <Label Text="{Binding .}" 
                                LineBreakMode="NoWrap" VerticalTextAlignment="Center" HorizontalTextAlignment="Center"
                                Style="{DynamicResource ListItemTextStyle}" 
                                FontSize="16" />
                            </StackLayout>
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>